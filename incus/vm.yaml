config:
  limits.cpu: "2"
  limits.memory: "2048MiB"
  boot.autostart: "true"
devices:
  root:
    path: / # incus instance root
    pool: default # incus profile default
    type: disk
    size: "12GiB"
description: "Incus Ubuntu system containers configuration"
